### force files MIME type, encoding, & set cache to 10 minutes
<FilesMatch "^(___|email|tel)$">
  ForceType "text/html; charset=utf-8"
  Header set Content-Encoding "deflate"
  Header set Cache-Control "max-age=601, public, proxy-revalidate"
</FilesMatch>
